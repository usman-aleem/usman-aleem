Drop real screenshots here, named exactly to match each project slug:

  crochet-by-urooj.jpg
  azu-wears.jpg
  sakinah.jpg
  younas-sweet-bakers.jpg

That's it — no code changes needed. The site checks for these files automatically:
if a file exists, the real screenshot replaces the gradient placeholder for that project.
If it's missing, the gradient placeholder keeps showing (site never breaks).

Tips for the screenshot itself:
- Landscape or square works best (the panel crops to fill, roughly 16:9 to 4:3).
- A clean app screen (product listing, dashboard, hero screen) reads better than a
  cluttered debug view or emulator chrome — crop tight to just the UI.
- JPG or PNG both work — just match the filename above exactly (case-sensitive).
